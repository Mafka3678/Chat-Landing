# ChatLanding Pro - AI Powered 🚀

Высококонверсионный чат-лендинг с интеграцией Google Gemini AI для автоматической квалификации лидов.

## ✨ Особенности

- **Интеллектуальный диалог**: Использует модель `gemini-3-flash-preview` для естественного общения.
- **Динамические быстрые ответы**: ИИ автоматически генерирует варианты ответов для пользователя в зависимости от контекста.
- **Квалификация лидов**: Пошаговый сценарий (Ниша -> Бюджет -> Планы -> Контакты).
- **Современный UI**: Анимации, glassmorphism, адаптивный дизайн на Tailwind CSS.
- **Безопасность**: API ключи не хранятся в коде (используются переменные окружения).

## 🛠 Требования

- [Node.js](https://nodejs.org/) (v18+)
- API ключ Google Gemini ([получить здесь](https://aistudio.google.com/))

## 🚀 Быстрый старт

### 1. Установка

Распакуйте архив и установите зависимости:

```bash
npm install
```

### 2. Настройка

Создайте файл `.env` в корне проекта:

```bash
cp .env.example .env
```

Добавьте ваш API ключ в `.env`:

```env
GEMINI_API_KEY=ваш_ключ_здесь
```

### 3. Запуск

```bash
npm run dev
```
Откройте `http://localhost:3000` в браузере.

## ⚙️ Настройка логики бота

Вы можете изменить сценарий диалога и системные инструкции в файле:
`src/services/geminiService.ts`

Объект `stepInstructions` содержит промпты для каждого шага воронки:

```typescript
const stepInstructions: Record<ChatStep, string> = {
  [ChatStep.WELCOME]: "...", // Приветствие
  [ChatStep.BENEFITS]: "...", // Презентация
  // ... и так далее
};
```

## 📦 Сборка для продакшена

```bash
npm run build
```
Результат будет в папке `dist`.

## ZE Структура проекта

- `App.tsx` - Основная логика чата и UI.
- `services/geminiService.ts` - Интеграция с Gemini API и промпты.
- `components/ChatMessage.tsx` - Компонент сообщения.
- `types.ts` - TypeScript типы.
